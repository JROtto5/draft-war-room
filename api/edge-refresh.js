const crypto=require('node:crypto');
module.exports=async(req,res)=>{res.setHeader('Content-Type','application/json');res.setHeader('Cache-Control','no-store');const expected=process.env.SEASON_SCAN_KEY||'',actual=(req.headers.authorization||'').replace(/^Bearer /,'');if(!expected||Buffer.byteLength(expected)!==Buffer.byteLength(actual)||!crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(actual))){res.statusCode=401;return res.end(JSON.stringify({error:'Unauthorized'}));}try{const r=await fetch('https://nfl-edge-kappa.vercel.app/api/tick',{signal:AbortSignal.timeout(55000)});if(!r.ok)throw Error();res.end(JSON.stringify({ok:true,result:await r.json()}));}catch(e){res.statusCode=502;res.end(JSON.stringify({error:'Edge refresh unavailable; next scheduled run will retry.'}));}};
