import assert from 'node:assert/strict';import vm from 'node:vm';import {readFileSync} from 'node:fs';import {createRequire} from 'node:module';const require=createRequire(import.meta.url);const {day}=require('../api/party-parlays.js');
assert.equal(day('2026-09-10T00:20:00Z'),'2026-09-09');assert.equal(day('2026-09-10T05:00:00Z'),'2026-09-10');
const ctx={document:{getElementById:()=>({addEventListener(){}}),addEventListener(){}},setInterval(){}};vm.createContext(ctx);vm.runInContext(readFileSync('party.js','utf8'),ctx);const calc=n=>vm.runInContext('partyReturn('+n+')',ctx);assert.equal(calc(1800).total,95);assert.equal(calc(1800).profit,90);assert.equal(calc(-200).total,7.5);assert.equal(calc(0),null);assert.equal(calc(50),null);assert.equal(calc('Infinity'),null);assert.ok(Math.abs(calc(1800).implied-100/19)<1e-10);console.log('Party parlay ET date boundaries and quote-only fixed-stake payout tests passed');
